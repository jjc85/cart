nelmio_api_doc:
    documentation:
        info:
            title: Carrito de la compra
            description: Gestión de carrito de la compra
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
